[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "trainmeet-server"
version = "0.3.0"
description = "Local TrainMeet Server and server-authoritative Tambox runtime for Raspberry Pi"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
mqtt = ["paho-mqtt>=2.1,<3"]

[project.scripts]
trainmeet-server = "tambox_gateway.local_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tambox_gateway = ["web/*.html", "web/*.css", "web/*.js"]
